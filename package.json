{
  "name": "cloudflare-dns-mcp-server",
  "version": "1.0.0",
  "description": "MCP server (streamable HTTP) for managing Cloudflare DNS records from any MCP-compatible LLM client",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "cloudflare-dns-mcp-server": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "smoke": "node scripts/smoke.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "express": "^4.21.2",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
